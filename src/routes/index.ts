import { default as client } from './client';
import { default as oauth2 } from './oauth2';
import { default as auth } from './auth';
import { default as user } from './user';

export default {
  client,
  oauth2,
  auth,
  user,
};
